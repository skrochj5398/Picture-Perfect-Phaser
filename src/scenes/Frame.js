class Frame{
    
    Frame(picFrame){
        this.Frame = picFrame
    }
}
 
/*const picFrame = new picFrame(this.add.image(CONFIG.DEFAULT_WIDTH / 1.98, CONFIG.DEFAULT_HEIGHT / 1.96, 'Frame'))
    Frame.setScale(
  CONFIG.DEFAULT_WIDTH / Frame.width * 1.05,
  CONFIG.DEFAULT_HEIGHT / Frame.height * 1.16
)*/






export default Frame
